config:
  target: "http://localhost:8080"
  phases:
    - duration: 30
      arrivalRate: 5

scenarios:
  - name: "Login test"
    flow:
      - post:
          url: "/api/auth/login"
          json:
            email: "matias.benitez@barberpro.com"
            password: "Admin123!"