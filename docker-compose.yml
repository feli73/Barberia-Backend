services:
  api:
    container_name: barberia_api
    build:
      context: .
      dockerfile: Dockerfile
    image: barberia-api:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped